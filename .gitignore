# Session state, crash logs and layout dumps - all regenerated at runtime.
state_*

Exports/*
!Exports/.gitkeep

# Build output. The sources in app/ are what is tracked; the packaged build is
# ~300MB of bundled Chromium, rebuilt with `npm run build` in app/.
dist/
*.pdb

config/render.conf
